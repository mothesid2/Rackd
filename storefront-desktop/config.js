// URLs of the DEPLOYED web storefront (Cloudflare Pages). Fill these in after the
// storefront deploy. The Production build loads PROD_URL; the Demo build loads
// DEMO_URL (the preview/sandbox deployment) and shows a DEMO badge.
module.exports = {
  PROD_URL: 'https://order.rackd.com',        // production storefront (real data)
  DEMO_URL: 'https://demo.rackd-storefront.pages.dev', // preview/demo (sandbox)
};
