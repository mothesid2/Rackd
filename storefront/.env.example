# Public config (safe to expose — anon key is RLS-protected, Stripe pk is publishable).
# Copy to .env.local. The Supabase values are the same RackD project the POS uses.
NEXT_PUBLIC_SUPABASE_URL=https://zougcsomgqnfumqkwuoj.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
